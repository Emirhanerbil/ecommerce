import 'package:flutter/material.dart';

class PagePadding {
  final pagePadding = const EdgeInsets.symmetric(horizontal: 16, vertical: 16);
  final pagePaddingHorizontal = const EdgeInsets.only(top: 24);
}

class BoxPadding {
  final boxPadding =
      const EdgeInsets.only(left: 24, right: 36, top: 14, bottom: 18);
}
